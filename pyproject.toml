[project]
name = "agents"
version = "0.1.0"
description = "LangChain agent project"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "langchain>=1.0.0",
    "langchain-openai>=1.0.0",
    "langgraph>=0.2.0",
    "python-dotenv>=1.0.0",
]
